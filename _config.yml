title: "Carlos Pérez | Network Engineering Notes"
description: "Notas sobre ingeniería de redes, automatización, monitoreo y peering."
theme: minima

markdown: kramdown
kramdown:
  input: GFM

plugins:
  - jekyll-feed

# 👇 Agrega esto al final del archivo
defaults:
  - scope:
      path: ""          # raíz del sitio, idioma por defecto (español)
    values:
      lang: es
  - scope:
      path: "en"        # todo lo que esté dentro de /en/ hereda lang: en
    values:
      lang: en
